{
  "name": "smartchat-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "install-all": "npm install && npm install --workspace=client",
    "build": "npm run build --workspace=client"
  },
  "workspaces": [
    "client"
  ],
  "dependencies": {
    "@supabase/supabase-js": "^2.38.4",
    "node-fetch": "^2.6.7"
  }
}